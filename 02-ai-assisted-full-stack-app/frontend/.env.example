# Backend URL that the Vite dev server proxies /api/* requests to.
# Only needed if the FastAPI backend is not on http://localhost:8000.
# Copy this file to .env and adjust.
VITE_BACKEND_URL=http://localhost:8000
